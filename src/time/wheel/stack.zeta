// Auto-converted from /tmp/df-tokio-util-0.7.18/tokio-util-0.7.18/src/time/wheel/stack.rs

use std::borrow::Borrow;
use std::cmp::Eq;
use std::hash::Hash;
/// Abstracts the stack operations needed to track timeouts.
trait Stack {
 type Owned;
 type Borrowed;
 type Store;
 fn is_empty(&self) -> bool;
 fn push(&mut self, item: Self::Owned, store: &mut Self::Store);
 fn pop(&mut self, store: &mut Self::Store) -> Option<Self::Owned>;
 fn peek(&self) -> Option<Self::Owned>;
 fn remove(&mut self, item: &Self::Borrowed, store: &mut Self::Store);
 fn when(item: &Self::Borrowed, store: &Self::Store) -> u64
}


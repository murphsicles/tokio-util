// Auto-converted from /tmp/df-tokio-util-0.7.18/tokio-util-0.7.18/src/loom.rs

mod sync {
 #[cfg(all(test, loom))] pub(crate) use loom::sync::{ Arc, Mutex, MutexGuard };
 #[cfg(not(all(test, loom)))] pub(crate) use std::sync::{ Arc, Mutex, MutexGuard }
}

